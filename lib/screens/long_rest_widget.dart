import 'package:flutter/material.dart';

class LongRestPage extends StatelessWidget {
  const LongRestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}