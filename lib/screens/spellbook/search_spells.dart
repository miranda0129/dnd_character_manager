import 'package:flutter/material.dart';

class SearchSpells extends StatelessWidget {
  const SearchSpells({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}