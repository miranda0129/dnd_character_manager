import 'package:flutter/material.dart';

class ShortRestPage extends StatelessWidget {
  const ShortRestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}